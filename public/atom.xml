<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[TRACTATUS ELEMENTALIS]]></title>
  <link href="https://tractatuslapipaplena.herokuapp.com/atom.xml" rel="self"/>
  <link href="https://tractatuslapipaplena.herokuapp.com/"/>
  <updated>2014-05-17T04:43:14+02:00</updated>
  <id>https://tractatuslapipaplena.herokuapp.com/</id>
  <author>
    <name><![CDATA[templix]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[lshell]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/lshell/"/>
    <updated>2014-05-17T04:43:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/lshell</id>
    <content type="html"><![CDATA[<p>shell restrictiva para permitir a usuarios acceder via ssh al sistema limitando sus privilegios. Algunas modificaciones en el archivo de configuración:</p>

<blockquote><p># nano /etc/lshell.conf</p></blockquote>

<pre><code>...

[global]

# archivo de logs

logpath         : /var/log/lshell/

...

[default]

...

# Comandos permitidos

allowed         : ['ls','echo','cd','ll','cat','more','nano']

# Mensaje al arrancar

intro           : "*** Entrando en la máquina con privilegios limitados *** Pulsar '?' o 'help' para listar comandos autoritzados"

# Si el usuario tiene una carpeta personal

home_path       : '/home/USUARIO/'
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[apt-mirror]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/apt-mirror/"/>
    <updated>2014-05-17T04:43:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/apt-mirror</id>
    <content type="html"><![CDATA[<p>[apache2]. Configurar un repositorio local. Seguidamente se muestra el proceso:</p>

<blockquote><p>$ mkdir repo (Crear el directorio)</p>

<p># nano /etc/apt/mirror.list</p></blockquote>

<p>Añadir al principio la linea del directorio:</p>

<pre><code>set base_path /home/USER/repo
</code></pre>

<blockquote><p># apt-mirror (Recargar/actualizar el repositorio)</p></blockquote>

<p>Crear el enlace para que esté disponible via web:</p>

<blockquote><p>$ cd /var/www/html</p>

<p>$ ln -s /home/USER/distro/mirror/us.archive.ubuntu.com/ubuntu ubuntu</p></blockquote>

<p>Modificar el sources.list:</p>

<blockquote><p># nano /etc/apt/sources.list</p></blockquote>

<p>Añadir la lineas:</p>

<pre><code>deb http://192.168.1.2/ubuntu trusty universe

deb http://192.168.1.2/ubuntu trusty main restricted

deb http://192.168.1.2/ubuntu trusty-updates main restricted
</code></pre>

<blockquote><p># apt-update (Actualizar el cliente)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[typeset]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/typeset/"/>
    <updated>2014-05-07T18:24:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/typeset</id>
    <content type="html"><![CDATA[<p>Define variables y les da atributos. Es necesario para operar con números y hacer cuentas con ellos. Ver &#8220;declare&#8221;</p>

<p>Un ejemplo de uso en un script:</p>

<pre><code>#! /bin/bash

typeset -i suma

suma=7+5

echo $suma
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[boinc]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/boinc/"/>
    <updated>2014-05-05T21:18:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/boinc</id>
    <content type="html"><![CDATA[<p>[boinc-client boinc-manager]Plataforma de software libre para la computación distribuida que comprende áreas tan diversas como matemáticas, medicina, biologia, astrofísica&#8230; Es una forma de aprovechar nuestros equipos durante los periodos de inactividad para que sean usados en el marco de investigaciones cientificas.</p>

<blockquote><p># /etc/init.d/boinc-client start (Arrancar el demonio)</p>

<p>$ boincmgr (Arrancar la gui para la configuración inicial)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[bc]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/bc/"/>
    <updated>2014-05-05T16:12:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/bc</id>
    <content type="html"><![CDATA[<p>Calculadora</p>

<p>bc -l  (Entra en el promt de la calculadora. Para terminar teclear quit o exit)</p>

<blockquote><p>$ echo &#8220;36*2&#8221; | bc</p>

<p>$ echo &#8220;(1409302/28186042)*100&#8221; | bc -l</p>

<p>$ echo &#8220;scale = 6; 13.397/12&#8221; | bc (Resultado con 6 decimales)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[stat]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/stat/"/>
    <updated>2014-05-03T08:09:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/stat</id>
    <content type="html"><![CDATA[<p>Estadísticas de archivo, directorios y sistemas</p>

<blockquote><p># stat /etc/passwd</p>

<p># stat /usr</p>

<p># stat -f /</p>

<p>$ stat -c &#8220;%s&#8221; archivo (medida del archivo)</p>

<p>$ stat -c &#8216;%n %U:%G-%a&#8217; * (Mostrar nombre archivo, propietario, grupo y permisos)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[ftp]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/ftp/"/>
    <updated>2014-04-28T18:36:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/ftp</id>
    <content type="html"><![CDATA[<p>Conexión remota por linea de comandos</p>

<blockquote><p># ftp -help (Para obtener ayuda)</p>

<p># ftp -u usuario,clave ftp.dominio.com (Para conectarnos con un servidor)</p>

<p># ftp dominio.com 20201 (Entrar especificando un puerto)</p></blockquote>

<p>Al entrar en el directorio raiz del servidor ftp remoto con un prompt al estilo de:</p>

<p>ftp usuario@ftp.dominio.com:~></p>

<p>Algunas opciones:</p>

<pre><code>get archivo (Recibir un fichero de la máquina remota)

mget file file file (Recibir varios ficheros de la máquina remota.

put archivo (Enviar un fichero desde nuestra máquina a la máquina remota)

mput file file file (Enviar varios archivos de nuestra máquina a la máquina remota)

quit (Para desconectarnos del servidor)

!ls (Ejecuta una shell que permite la entrada de comandos [ls] en nuestro sistema. Con "exit" volveremos al programa de FTP)

!xterm &amp; (Lanzaría otra xterm)

bell (Indica al sistema que ejecute un pitido cuando finalice la ejecución de los comandos)

bye (Termina la ejecución de ftp)

quit (Termina la ejecución de ftp)

close (Termina la sesión ftp sin salir del programa)

delete file (Borra un archivo)

mdelete file file file (Borra varios archivos)

reget file(Continuar bajando un fichero cortado anteriormente)

open (conectar desde un ftp a otro)

? (Muestra todos los comandos disponibles)

cd, dir, ls, list, mkdir, rmdir, pwd, rename (Funionan igual que en la shell)
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[fatattr]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/fatattr/"/>
    <updated>2014-04-28T05:49:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/fatattr</id>
    <content type="html"><![CDATA[<p>Permite ver y/o modificar los atributos MSDOS en un sistema de archivos FAT empleando los signos + para otorgar y - para quitar. Dichos premisos son:</p>

<pre><code>r (Solo lectura)

a (Archivo)

s (Archivo del sistema)

h (Oculto)

d (Directorio)

v (Etiqueta de volumen)
</code></pre>

<blockquote><p>$ fatattr /media/usb1/* (Muestra los atributos)</p>

<p>$ fatattr -s /media/usb1/* (Quita el atributo oculto de todo el usb)</p>

<p>$ fatattr +s /media/usb1/* (Oculta todos los archivos del usb)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[update-manager]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/update-manager/"/>
    <updated>2014-04-26T12:48:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/update-manager</id>
    <content type="html"><![CDATA[<p>Actualizar, en Ubuntu, a una nueva versión. Ver también do-release-upgrade.</p>

<blockquote><p># update-manager -d   (antes de salir la version estable final)</p>

<p># update-manager -c   (despues de salir la version estable final)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[do-release-upgrade]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/do-release-upgrade/"/>
    <updated>2014-04-26T12:48:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/do-release-upgrade</id>
    <content type="html"><![CDATA[<p>Herramienta para actualizar por consola sistemas basados en Ubuntu a una nueva versión de la distribución. Para configurar si queremos actualizar entre versiones normales o versiones LTS, debemos modificar del fichero /etc/update-manager/release-upgrades la linea:</p>

<p>Para versiones LTS:</p>

<p>Prompt=lts</p>

<p>Para versiones normales:</p>

<p>Prompt=normal</p>

<p>Para no verificar nunca nuevas versiones:</p>

<p>Prompt=never</p>

<p>Estas modificaciones son válidas también para update-manager [ver]</p>

<blockquote><p># do-release-upgrade -d</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[slackpkg]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/slackpkg/"/>
    <updated>2014-04-26T07:29:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/slackpkg</id>
    <content type="html"><![CDATA[<p>Gestor de paquetes de Slackware sin resolución de dependencias. Como previa antes de su uso es preciso descomentar el repositorio de nuestra elección en /etc/slackpkg/mirrors</p>

<blockquote><p># slackpkg update gpg (Bajar llaves publicas)</p>

<p># slackpkg update (Recargar listado)</p>

<p># slackpkg upgrade-all (Actualizar toda la distro)</p>

<p># slackpkg search paquete (buscar paquete)</p>

<p># slackpkg install paquete (instalar paquete)</p>

<p># slackpkg upgrade paquete (actualiza el paquete elegido)</p>

<p># slackpkg reinstall paquete (reinstala paquete)</p>

<p># slackpkg remove paquete (desinstala paquete)</p>

<p># slackpkg blacklist paquete (pone el paquete en lista negra)</p>

<p># slackpkg download paquete (descarga paquete sin instalarlo)</p>

<p># slackpkg info paquete (muestra informacion de paquete)</p>

<p># slackpkg clean-system (Lista los paquetes obsoletos)</p>

<p># slackpkg install-new (Lista los paquetes introducidos recientemente)</p>

<p># slackpkg generate-template (Generar una plantilla de los paquetes instalados en un pc)</p>

<p># slackpkg install-template (Instalar un slackware en un pc a partir de una plantilla))</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[lxde]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/lxde/"/>
    <updated>2014-04-26T06:37:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/lxde</id>
    <content type="html"><![CDATA[<p>Escritorio ligero. Sus componentes:</p>

<pre><code>pcmanfm (gestor de archivos)

lxlauncher (lanzador de aplicaciones)

lxpanel (panel de escritorio)

lxsession-edit (cambiar el gestor de ventanas y las aplicaciones ejecutadas al inicio)

lxsession  (gestor de sesiones con las opciones cerrar, reiniciar y suspender)

lxappearance  (gestor de temas. Permite cambiar el aspecto, iconos  y tipografías)

leafpad (editor de texto)

xarchiver  (gestor de archivos comprimidos)

gpicview (visor de imágenes)

lxterminal (Consola)

lxtask  (administrador de tareas y monitor de sistema)

openbox  (gestor de ventanas)

lxrandr (gestor de resolucaión de pantalla y monitores externos)

lxshortcut (editor de lanzadores)

lxmusic (reproductor de música basado en xmms2)
</code></pre>

<p>Archivos de configuración de los elementos del escritorio lxde:</p>

<pre><code>~/.config/gtkrc-2.0 (Temas de escritorio GTK+ e iconos)

~/.config/leafpad/leafpadrc (Editor de textos Leafpad)

~/.config/lxpanel/LXDE/panels/panel (Paneles)

~/.config/lxpanel/LXDE/config (Lanzadores de aplicaciones del panel)

~/.config/lxsession/LXDE/config (Administrador de ventanas)

~/.config/lxterminal/lxterminal.conf (Terminal)

~/.config/openbox/lxde-rc.xml (Openbox)

~/.config/openbox/menu.xml (Menu de Openbox)

~/.config/pcmanfm/main.lxde (Pcmanfm)
</code></pre>

<p>1.-</p>

<p>Arrancar aplicaciones al inicio:</p>

<blockquote><p># /etc/xdg/lxsession/LXDE/autostart</p></blockquote>

<p>Colocar las aplicaciones precedidas de una @:</p>

<pre><code>@liferea

@gdesklets
</code></pre>

<p>2.-</p>

<p>Crear iconos en el escritorio:</p>

<p>Colocar los archivos en la carpeta Desktop o Escritorio si se tiene en español:</p>

<blockquote><p>$ nano Desktop/home.lnk</p></blockquote>

<p>Y colocamos el siguiente contenido de ejemplo:</p>

<pre><code>[Desktop Entry]

Name=Personal

Type=Application

Comment=Carpeta personal

Terminal=false

Exec=pcmanfm ~

Icon=folder_home.png

Encoding=UTF-8
</code></pre>

<p>Para colocar la papelera:</p>

<pre><code>$ nano Desktop/papelera.lnk

Y pegar el contenido:

[Desktop Entry]

Name=Papelera

Type=Application

Comment=Papelera

Terminal=false

Exec=pcmanfm trash:///

Icon=emptytrash.png

Encoding=UTF-8
</code></pre>

<p>3.-</p>

<p>Modificar iconos:</p>

<blockquote><p>$ ls /usr/share/icons/ (Ver el listado de los disponibles)</p>

<p># nano  /usr/share/icons/default/index.theme</p></blockquote>

<p>Y en apartado Inherits colocar el deseado:</p>

<p>Inherits = ComixCursors-Red-Large</p>

<p>Reiniciar.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[virtualbox]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/virtualbox/"/>
    <updated>2014-04-22T05:56:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/virtualbox</id>
    <content type="html"><![CDATA[<p>[vitualbox-guest-utils linux-headers-$(uname -r) build-essential dkms]. Herramienta que permite la virtualización de sistemas operativos.</p>

<p>En la página https://www.virtualbox.org/wiki/Downloads apartado &#8220;VirtualBox 4.2 Oracle VM VirtualBox Extension Pack&#8221; clicar en &#8220;All platforms&#8221; y Descargar el paquete de extensiones Oracle_VM_VirtualBox_Extension_Pack-4.2.0-80737.vbox-extpack. Para instalarlo, doble clic en el paquete y seguir instrucciones [aceptar condiciones y contraseña de root].</p>

<blockquote><p># usermod -aG vboxusers USUARIO (Agregar usuario al grupo vboxusers)</p>

<p># gpasswd -a $USER vboxsf (Agregar usuario al grupo vboxsf)</p>

<p># modprobe vboxdrv (Activar módulo)</p>

<p>$ VBoxManage extpack install Oracle_VM_VirtualBox_Extension_Pack-4.2.16-86992.vbox-extpack (instala un pack de extensiones)</p>

<p>$ VBoxManage list extpacks  (Ver paquetes de expansión instalados)</p></blockquote>

<p>1.-</p>

<p>Gestión de máquina virtuales desde consola:</p>

<p>Para la gestión de máquinas virtuales sin interfaz gráfica, se utiliza VboxHeadless para tal proposito que incluye diferentes comandos que veremos en las próximas líneas.</p>

<blockquote><p>$ VBoxManage createvm &#8211;name &#8220;maquina_virtual&#8221; &#8211;register (Creamos una máquina virtual con el nombre &#8220;maquina_virtual&#8221;)</p>

<p>$ VBoxManage modifyvm &#8220;maquina_virtual&#8221; &#8211;memory 780 &#8211;acpi on &#8211;boot1 dvd &#8211;nic1 bridged &#8211;bridgeadapter1 eth0 &#8211;ostype Ubuntu (Modificamos la máquina virtual que hemos creado, asignándole 780MB de RAM, activando ACPI, definiendo que arranque desde CD/DVD, indicando que la tarjeta de red sea en modo bridge y que el la tarjeta de red del sistema a usar es la eth0, y finalmente definiendo que la máquina será un Ubuntu.)</p>

<p>$ VBoxManage createvdi &#8211;filename ~/VirtualBox\VMs/maquina_virtual/maquina_virtual-disk01.vdi &#8211;size 30000 (Creamos un disco duro en formato VDI de 30 GB)</p>

<p>$ VBoxManage storagectl &#8220;maquina_virtual&#8221; &#8211;name &#8220;IDE Controller&#8221; &#8211;add ide (Añadimos un controlador IDE para posteriormente conectar el disco duro)</p>

<p>$ VBoxManage storageattach &#8220;maquina_virtual&#8221; &#8211;storagectl &#8220;IDE Controller&#8221; &#8211;port 0 &#8211;device 0 &#8211;type hdd &#8211;medium ~/VirtualBox\VMs/maquina_virtual/maquina_virtual-disk01.vdi (Asignamos el disco duro a la máquina virtual)</p>

<p>$ VBoxManage storageattach &#8220;maquina_virtual&#8221; &#8211;storagectl &#8220;IDE Controller&#8221; &#8211;port 1 &#8211;device 0 &#8211;type dvddrive &#8211;medium ~/IS</p></blockquote>

<p>O/ubuntu-12.04.2-server-i386.iso (Asignamos a la máquina Virtual una ISO de instalación, en este caso de Ubuntu Server)</p>

<blockquote><p>$ VBoxManage modifyvm &#8220;maquina_virtual&#8221; &#8211;pae on (activamos PAE en la máquina Virtual)</p>

<p>$ VBoxManage modifyvm &#8220;maquina_virtual&#8221; &#8211;memory 512 (Modificamos la RAM a 512MB)</p>

<p>$ VBoxHeadless &#8211;startvm &#8220;maquina_virtual&#8221; (arranca una máquina virtual)</p>

<p>$ VBoxHeadless &#8211;startvm &#8220;maquina_virtual&#8221; -e &#8220;TCP/Ports=7000&#8221; &amp; (Arrancamos la máquina virtual indicando que queremos un servidor RDP para conexión remota en el puerto 7000)</p>

<p>$ VBoxManage controlvm &#8220;maquina_virtual&#8221; poweroff (Para la máquina virtual. Recomendable parar primero la máquina virtual desde la maquina virtual y luego ejecutar esto)</p>

<p>$ VBoxManage clonehd Metasploitable.vmdk ../Metasploitable2-Linux.vdi -format VDI (Convertir una imagen vmdk en vdi)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[automater]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/automater/"/>
    <updated>2014-04-21T12:33:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/automater</id>
    <content type="html"><![CDATA[<p>(python3-httplib2 python2.6-argparse)</p>

<p>Obtener información de ips y urls inclusive si estan bloqueadas en algún blocklist. Copiar el script en python de: <a href="https://github.com/1aN0rmus/TekDefense-Automater.git">https://github.com/1aN0rmus/TekDefense-Automater.git</a></p>

<blockquote><p>$ python automater.py IP (De una ip)</p>

<p>$ python automater.py ips.txt (De un listado de ips, una por linea)</p>

<p>$ python automater.py 44A6A7D4A039F7CC2DB6E85601F6D8C1 (De un hash)</p>

<p>$ python automater.py ips.txt -o informe.txt -w informe.html -c informe.csv (Enviando el resultado a un archivo de texto y a una en html y a uno csv)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[cadaver]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/cadaver/"/>
    <updated>2014-04-20T12:16:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/cadaver</id>
    <content type="html"><![CDATA[<p>Cliente de WebDAV para linux. El modulo WebDav es un método que permite compartir, editar y manejar archivos en un servidor remoto via HTTP.</p>

<blockquote><p>$ cadaver 192.168.2.25 (Conectar con un host)</p></blockquote>

<pre><code>dav:/&gt;
</code></pre>

<blockquote><p>$ cadaver https://www.sitio.com (Conectar a un host y requerimiento de usuario y contraseña)</p></blockquote>

<pre><code>Authentication required for webdav on server `sitio.com`:

Username: USER

Password: PASS

dav:/&gt;
</code></pre>

<p>Una vez conectado, algunos comandos disponibles son:</p>

<pre><code>put archivo (Subir un archivo)

get archivo (Descargar un archivo)

move viejo nuevo (Cambiar el nombre de viejo a nuevo)

ls (Listar contenido)

edit archivo (Editar un archivo)

pwd (Mostrar directorio donde nos encontramos)

cd dir (Entrar en un directorio)

? (Muestra todos los comandos disponibles)

quit (Salir)
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[apt-get]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/apt-get/"/>
    <updated>2014-04-20T11:16:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/apt-get</id>
    <content type="html"><![CDATA[<p>[aptitude y apt-cache]. Gestor de programario de distribuciones Debian. Listtado del historial de instalaciones y desinstalaciones en /var/log/apt/history.log</p>

<blockquote><p># apt-get update    (Actualiza la lista de paquetes)</p>

<p># apt-get upgrade   (Actualiza todos los paquetes instalados)</p>

<p># apt-get upgrade paquete     (Solo actualiza “paquete”)</p>

<p># apt-get dist-upgrade    (Actualiza la distribución)</p>

<p># apt-get install paquete (Instala paquetes con resolución de depencias)</p>

<p># pat-get install paquete -y (instalar paquete sin pedir confirmación)</p>

<p># apt-get install &#8211;reinstall  paquete      (Reinstala un paquete)</p>

<p># apt-get autoremove  (Borra paquetes huérfanos)</p>

<p># apt-get remove paquete        (Elimina un paquete)</p>

<p># apt-get remove &#8211;purge paquete  (Desinstala con archivos de configuración)</p>

<p># apt-get remove $(deborphan)  (Elimina huerfanos. lanzar varias veces)</p>

<p># apt-get clean        (Elimina caché)</p>

<p># apt-get autoclean  (lo mismo de versiones antiguas de paquetes)</p>

<p># apt-get -f install   (sin especificar paquete, para reparar la aplicación)</p>

<p># apt-get -s install paquete        (simula la instalación)</p>

<p># apt-get &#8211;compile source paquete  (baja las fuentes, compila y crear un .deb)</p>

<p># apt-get check          (Verifica dependencias rotas)</p>

<p># apt-get build-dep paquete (instalar las dependencias necesarias para poder compilar un paquete fuente)</p>

<p># apt-get install network-manager=0.9.1.90-0ubuntu3 (version concreta)</p>

<p># apt-get hold network-manager (Mantener una version concreta)</p>

<p># apt-get source paquete (buscar un paquete fuente para ser compilado)</p>

<p># apt-rdepends paquete (muestra todas las dependencias)</p>

<p># apt-rdepends -b paquete (muestra las dependencias directas)</p>

<p># apt-rdepends -f paquete  (muestra version y estado del paquete)</p>

<p># apt-cache search paquete       (Busca en la cache paquete)</p>

<p># apt-cache show paquete         (Muestra la descripción completa del paquete)</p>

<p># apt-cache showpkg paquete   (Muestra su relación con otros paquetes)</p>

<p># apt-cache depends paquete    (Muestra las dependencias del paquete)</p>

<p># apt-cache rdepend paquete     (Muestra dependencias inversas del paquete)</p>

<p># apt-cache policy paquete       (repositorio de donde se ha bajado un paquete)</p>

<p># apt-get &#8211;yes install paquete (Responder “si” a preguntas de la instalación)</p>

<p># aptitude search ~ilinux-image (Lista los kernels instalados)</p>

<p># aptitude search ~i -F %p (Lista los paquetes instalados)</p>

<p># apt-setup                              (Crea un sources.list optimizado)</p>

<p># add-apt-repository ppa:repositorio   (añadir un repositorio ppa)</p>

<p># ppa-purge ppa:repositorio   (suprimirlo. Precisa el script ppa-purge))</p></blockquote>

<p>/etc/apt/sources.list               (Fichero de repositorios.)</p>

<p>/var/cache/apt/archives          (Caché de los paquetes .deb instalados)</p>

<p>/var/log/apt/term.log (Archivo de logs de la aplicación)</p>

<p>1.-</p>

<p>Para resolver problemas con  /var/lib/apt/lists/*  lanzar:</p>

<blockquote><p># rm /var/lib/apt/lists/* -vf</p></blockquote>

<p>Y luego:</p>

<blockquote><p># apt-get update</p></blockquote>

<p>2.-</p>

<blockquote><p># apt-get -o &#8220;Acquire::Check-Valid-Until=false&#8221; update   (Saltarse el validador  que verifica la fecha de caducidad del archivo “Release” de debian [Error ” Release file expired, ignoring file”])</p></blockquote>

<p>3.-</p>

<p>Añadir repositorios de distintas ramas en debian.</p>

<p>Incluir en el sources.list los repositorios de las distintas ramas:</p>

<blockquote><p># nano sources.list</p></blockquote>

<pre><code>## Debian Testing

deb http://ftp.de.debian.org/debian/ testing main contrib non-free

deb-src http://ftp.de.debian.org/debian/ testing main contrib non-free

## seguridad Debian Testing

deb http://security.debian.org/ testing/updates main contrib non-free

deb-src http://security.debian.org/ testing/updates main contrib non-free

## Debian Unstable

deb http://ftp.us.debian.org/debian unstable main non-free contrib

## Debian experimental

deb http://ftp.fr.debian.org/debian experimental main non-free contrib
</code></pre>

<p>Editar o crear el fichero:</p>

<blockquote><p># nano /etc/apt/apt.conf</p></blockquote>

<p>Con el siguiente contenido:</p>

<pre><code>APT::Default-Release “testing”;

APT::Cache-Limit 55000000;

Apt::Get::Purge;

APT::Clean-Installed;

APT::Get::Fix-Broken;

APT::Get::Fix-Missing;

APT::Get::Show-Upgraded “true”;
</code></pre>

<p>Con el que establecemos las siguientes caracteristicas: Definir Debian Testing como rama principal,Limitar la cache utilizada en el proceso de actualización,Borrar archivos de configuración de los paquetes desinstalados y Arreglar el sistema en caso de detectar dependencias rotas.</p>

<p>Editar el archivo preferences:</p>

<blockquote><p># nano /etc/apt/preferences</p></blockquote>

<p>Con el contenido:</p>

<pre><code>Package: *

Pin: release a=testing

Pin-Priority: 900

Package: *

Pin: release a=unstable

Pin-Priority: 600

Package: *

Pin: release a=experimental

Pin-Priority: 50
</code></pre>

<p>Que tiene por significado que si el paquete existe en la rama testing se enstalará este, en caso contrario, si existe en unstable se instalará el unstable y sólo si no existe en las otras dos, se instalará de la versión experimental.</p>

<p>También podemos especificarlo en el comando:</p>

<blockquote><p># apt-get install libreoffice/unstable (Firefox se instalará de la rma unstable, pero las dependencias, se buscarán en primer lugar en la rama testing)</p>

<p># apt-get -t unstable install firefox (Tanto firefox como las dependencias se instalarás de la rama unstable)</p></blockquote>

<p>4.-</p>

<p>Instalación de aplicaciones sin conexión a internet. Ha de instalarse en dos equipos: el que tiene conexión y en el que no la tiene. Esta aplicación tiene muchas limitaciones ya que no instala dependencias por lo que puede usarse en pocos casos.</p>

<p>En el equipo sin conexión:</p>

<blockquote><p># apt-offline set apt.sig &#8211;update &#8211;upgrade &#8211;install-packages PAQUETE (genera el archivo apt.sig que ha de trasladarse al equipo que tiene conexión)</p></blockquote>

<p>En el equipo conexión:</p>

<blockquote><p># apt-offline get apt.sig &#8211;threads 5 &#8211;bundle offline.zip (Crea el archivo offline.zip que se traslada al equipo sin conexión)</p></blockquote>

<p>Otra vez en el equipo sin conexión:</p>

<blockquote><p># apt-offline install offline.zip</p>

<p># apt-get install PAQUETE (Instalación del paquete)</p></blockquote>

<p>5.- Instalar aplicaciones de arquitectura 386 en equipos amd64:</p>

<blockquote><p># nano /etc/apt/sources.list</p></blockquote>

<p>Añadir las lineas:</p>

<pre><code>deb http://ftp.debian.org/debian/ wheezy-updates main contrib

deb http://http.us.debian.org/debian/ wheezy contrib non-free main
</code></pre>

<blockquote><p># apt-get update</p>

<p># apt-get upgrade</p></blockquote>

<p>Activar multiarquitectura:</p>

<blockquote><p># dpkg &#8211;add-architecture i386</p>

<p># apt-get update</p></blockquote>

<p>Instalar librerías base para los sistemas de 32 bits:</p>

<blockquote><p># apt-get install libc6:i386</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[hydra]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/hydra/"/>
    <updated>2014-04-19T06:24:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/hydra</id>
    <content type="html"><![CDATA[<p>Se utiliza para crackear los sistemas de login de diferentes servicios como HTTP, FTP, TELNET, IMAP, SMB, SSH, etc.</p>

<blockquote><p># hydra 192.168.1.5 -l admin -p 1234 (Una ip con el usuario &#8220;admin&#8221; con la clave &#8220;1234&#8221;)</p>

<p># hydra -M lista_ips ssh -s 22 (A un listado de ips a una por linea y sobre el puerto 22)</p>

<p># hydra 192.168.1.8 ftp -s 21 -l juan -P wordlist.txt (Una ip el puerto 21 del usuario &#8220;juan&#8221; y probando las contraseñas de un archivo de texto a una por linea)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[sed]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/sed/"/>
    <updated>2014-04-19T06:07:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/sed</id>
    <content type="html"><![CDATA[<p>Permite borrar líneas, registros o sustituir cadenas de caracteres dentro de las líneas.</p>

<p>Archivo base para ejemplo (Empleado &#8211; Precio/hora &#8211; Horas trabajadas – Cómo las cobra):</p>

<pre><code>$ cat lista

Juan Gomez 6 10 banco

Clara Fuentes 7 12 banco

Antonio Cano 6 13 metálico

José Bueno 7 0 banco

Matías Crespo 5 8 metálico

Isabel Ruiz 7 0 metálico

María Monzón 9 16 metálico
</code></pre>

<p>Mostrar la linea 4 del fichero:</p>

<pre><code>$ sed -n 4p lista
</code></pre>

<p>Mostrar la 4ª y la 5ª:</p>

<pre><code>$ sed -n 4,5p lista
</code></pre>

<p>Mostrar de la 3ª al final del fichero:</p>

<pre><code>$ sed -n '4,$p' lista
</code></pre>

<p>Mostrar desde la primera linea que empiece por “Ant” hasta el final:</p>

<pre><code>$ sed -n '/^\Ant/,$p' lista
</code></pre>

<p>Mostrar sólo los parrafos desde “Clara” hasta “Bueno”:</p>

<pre><code>$ sed -n '/Clara/,/Bueno/p' lista
</code></pre>

<p>Mostrar cada linea a doble espacio:</p>

<pre><code>$ sed G lista
</code></pre>

<p>Mostrar las lineas que cumplen el patrón “Ju” en alguno de sus campos:</p>

<pre><code>$  sed -n '/Ju/p' lista
</code></pre>

<p>Borrar la 4ª linea del fichero:</p>

<pre><code>$  sed '4d' lista
</code></pre>

<p>Borrar la última linea del fichero:</p>

<pre><code>$  sed -e \$d lista
</code></pre>

<p>Lo mismo:</p>

<pre><code>$ sed '$d' lista
</code></pre>

<p>Borrar las lineas que empiecen por &#8220;J&#8221;:</p>

<pre><code>$ sed -e '/J/d' lista
</code></pre>

<p>Borrar la segunda linea</p>

<pre><code>$ sed '2d' lista
</code></pre>

<p>Borrar la 1ª y la 2ª:</p>

<pre><code>$  sed '1,2d' lista
</code></pre>

<p>Borrar a partir del primer espacio en blanco:</p>

<pre><code>$ sed -e 's/ .*//' lista
</code></pre>

<p>Borrar desde la primera linea que empiece por &#8220;Ant&#8221; hasta el final:</p>

<pre><code>$ sed -e '/^\Ant/,$d' lista
</code></pre>

<p>Borrar desde la 2 linea hasta la que tenga &#8220;Ruiz&#8221;:</p>

<pre><code>$ sed '2,/Ruiz/d' lista
</code></pre>

<p>Borrar las lineas en blanco de un archivo (en &#8220;lista&#8221; no procede):</p>

<pre><code>$  sed '/^$/d lista
</code></pre>

<p>Cambiar &#8220;metálico&#8221; por &#8220;gratis&#8221; excepto en la 3 linea:</p>

<pre><code>$ sed '3!s/metálico/gratis/' lista
</code></pre>

<p>Cambiar &#8220;met&#8221; por &#8220;tem&#8221; en las lineas que no tengan &#8220;Cano&#8221;:</p>

<pre><code>$ sed '/Cano/!s/met/tem/' lista
</code></pre>

<p>Cambiar campos (banco por cheque):</p>

<pre><code>$ sed 's/banco/cheque/' lista
</code></pre>

<p>Cambiar las &#8220;J&#8221; y la &#8220;C&#8221; mayúsculas por minúsculas:</p>

<pre><code>$ sed 'y/J-C/j-c/' lista
</code></pre>

<p>Substituye las vocales por &#8220;u&#8221;:</p>

<pre><code>$ sed -e "s/[aeio]/u/g" lista
</code></pre>

<p>Substituir los espacios por tabulaciones:</p>

<pre><code>$ sed -e 's/ /\t/g' lista
</code></pre>

<p>Alinear al centro:</p>

<pre><code>$ sed -e :a -e 's/^.\{1,77\}$/ &amp;/;ta' -e 's/\( *\)\1/\1/' lista
</code></pre>

<p>Todas las opciones pueden usarse como tubería substituyendo &#8220;lista&#8221; por “cat lista |”:</p>

<pre><code>$ cat lista | sed -n '4,$p'
</code></pre>

<p>Otros ejemplos:</p>

<p>Mostrar lineas no comentadas de un archivo:</p>

<pre><code>$ sed -e '/^#/d' /etc/fstab
</code></pre>

<p>Substituir &#8220;foto&#8221; por &#8220;FOTO&#8221; en todo los archivos del directorio:</p>

<pre><code>$ ls * | sed -e 'p;s/foto/FOTO/' | xargs -n2 mv
</code></pre>

<p>Cambiar “Juan” por “Pedro” en todos los archivos .txt del presente directorio:</p>

<pre><code>$ sed -i 's/Juan/Pedro/g' *.txt
</code></pre>

<p>Mostrar sólo las lineas con 4 dígitos seguidos:</p>

<pre><code>$ sed '/[0-9]\{4\}/p' archivo
</code></pre>

<p>Borrar todos los espacios delante de cada linea:</p>

<pre><code>$sed 's/^[ ^t]*//' archivo
</code></pre>

<p>Borrar todos los espacios detras de cada linea:</p>

<pre><code>$ sed 's/[ ^t]*$//' archivo
</code></pre>

<p>Borrar todos los espacios delante y detras de cada linea:</p>

<pre><code>$ sed 's/^[ ^t]*//;s/[ ^]*$//' archivo
</code></pre>

<p>Cambiar los números 3333 por 3.333. El parámetro -r sirva para activar las expresiones regulares extendidas de sed:</p>

<pre><code>$ sed -r "s/\&lt;(reg|exp)[a-z]+/\U&amp;/g"
</code></pre>

<p>Contar lineas de un archivo [emulador de wc -l]:</p>

<pre><code>sed -n '$=' lista
</code></pre>

<p>Insertar 5 espacios en blanco delante de cada linea:</p>

<pre><code>$ sed 's/^/     /' lista
</code></pre>

<p>Insertar 1 linea en blanco despues de la primera linea:</p>

<pre><code>$ sed '2i\ \' lista
</code></pre>

<p>Insertar 1 linea en blanco despues de la primera linea y de la tercera:</p>

<pre><code>$ sed '2i\ \' | sed '4i\ \' lista
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[john]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/john/"/>
    <updated>2014-04-17T19:49:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/john</id>
    <content type="html"><![CDATA[<p>Herramienta que permite averiguar contraseñas encriptadas</p>

<p>El de los repositorios acostumbra a estar capado. Descargar el programa y el parche de:</p>

<blockquote><p>$ wget http://www.openwall.com/john/g/john-1.7.3.4.tar.gz</p>

<p>$ wget http://openwall.info/wiki/_media/john/john-1.7.3.1-generic-crypt-1.diff.gz</p></blockquote>

<p>Descomprimir:</p>

<blockquote><p># tar xvzf john-1.7.3.4.tar.gz</p></blockquote>

<p>Entrar al directorio del programa:</p>

<blockquote><p># cd john-1.7.3.4</p></blockquote>

<p>Aplicar el parche:</p>

<blockquote><p># gzip -dc ../john-1.7.3.1-generic-crypt-1.diff.gz | patch -p1</p></blockquote>

<p>patching file src/Makefile</p>

<p>patching file src/crypt_fmt.c</p>

<p>patching file src/john.c</p>

<p>Hunk #2 succeeded at 67 (offset 2 lines).</p>

<p>El directorio donde se encuentra el código fuente es src:</p>

<blockquote><p># cd src</p>

<p># make</p></blockquote>

<p>Con make obtendrá una lista de los sistemas soportados, luego:</p>

<blockquote><p># make linux-x86-64</p></blockquote>

<p>Si el sistema no esta en la lista:</p>

<blockquote><p># make clean generic</p></blockquote>

<p>Pruebas de detección de correcto funcionamiento.</p>

<blockquote><p># cd ../run</p>

<p># ./john &#8211;test</p></blockquote>

<p>El fichero john.pot contiene los resultados encontrados por john. Si el fichero no está vacío significa que john ha encontrado la contraseña. El fichero john.rec contiene el estado actual de la búsqueda por si se produce una interrupción y es utilizado por el programa para reanudar la búsqueda.</p>

<p>1.-</p>

<p>Comprobación de la fortaleza de nuestra constraseñas:</p>

<blockquote><p># unshadow /etc/passwd /etc/shadow > contraseñas.txt</p>

<p># john contraseñas.txt</p>

<p># john &#8211;single contraseñas.txt (usanólo el modo single)</p>

<p># john &#8211;incremental contraseñas.txt (Usando el modo incremental)</p>

<p># john &#8211;wordlist=diccionario.lst &#8211;rules contraseñas.txt (Usando un diccionario)</p>

<p># john &#8211;show contraseñas.txt (Ver resultados)</p></blockquote>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[find]]></title>
    <link href="https://tractatuslapipaplena.herokuapp.com/blog/find/"/>
    <updated>2014-04-17T19:49:00+02:00</updated>
    <id>https://tractatuslapipaplena.herokuapp.com/blog/find</id>
    <content type="html"><![CDATA[<p>Buscar archivos o directorios</p>

<blockquote><p>$ find / -iname archivo     (Busca en / archivo sin distinguir minusculas y mayusculas)</p>

<p>$ find /home -name *.png -print      (Buscará en /home todos los archivos “.png” y mostrará la ruta)</p>

<p>$ find . -name &#8220;*.bak&#8221; -exec rm {} \; (buscará en el directorio todos los acabados en .bak y los borrará. la instrucción &#8220;-exec&#8221; seguido del comando a ejecutar. los corchetes {} se refiere a los ficheros que find ha encontrado y la barra invertida seguido de punto y coma \; indica el final de la instrucción)</p>

<p>$ find . ! -iname “<em>.png” ! -name “</em>.jpg”  (Buscar en el directorio actual todos los archivos menos los acabados en png y jpg)</p>

<p>$ find . -amin -15 (Buscar los que se ha accedido en los últimos 15 minutos)</p>

<p>$ find . -atime 15 (Buscar los que se accedió por última vez hace 15 dias)</p>

<p>$ find / -amin -10 (Los que se ha accedido los últimos 10 minutos)</p>

<p>$ find . -type f  (Buscar ficheros)</p>

<p>$ find . -type d  (Buscar directorios)</p>

<p>$ find / -iname fichero 2>/dev/null  (buscar redireccionando los avisos de permiso denegado a /dev/null)</p>

<p>$ find . -iname &#8221;<em>.txt&#8221; -or -iname &#8221;</em>.odt&#8221; -exec cp {} documentos \; (Buscar los txt y los odt y copiarlos a la carpeta documentos)</p>

<p>$ find . -type f -iname &#8220;*.bak&#8221; | xargs rm (buscar los ficheros acabados en .bak y borrarlos. xargs hace lo mismo que exec pero usando una tuberia.)</p>

<p>$ find /home/usuario -iname &#8216;*.bak&#8217; -delete (Otra forma de borrar todos los .bak de un directorio)</p>

<p>$ find . -type f -iname *.sla (buscará los ficheros .sla)</p>

<p>$ find . -type f -mtime 0 (Buscará los ficheros creados o modificados hoy)</p>

<p>$ find / -type f -mtime -1 -print (Los modificados las últimas 24 horas)</p>

<p>$ find / -mtime +2 -print (Los modificados hace más de dos dias)</p>

<p>$ find -L . -type l  (Buscar enlaces rotos)</p>

<p>$ find . -type f -size +300k > masde300k.txt (buscar .sh mayores de 300k)</p>

<p>$ find ./ -name .svn -exec rm -rf {} \; (eliminar todos los directorios y subdirectorios subversión)</p>

<p>$ find . -name &#8220;*.html&#8221; -exec grep -i -H &#8220;Tractatus elementalis&#8221; {} \; (Buscar en todos los archivos html del directorio actual con un texto concreto)</p>

<p>$ find . -name *.txt -exec sed -i &#8220;s/cosa/casa/g&#8221; {} \; (Substituir “cosa” por “casa” en todos los .txt del presente directorio)</p>

<p>$ find ~/ -depth -type f -empty (Buscar archivos vacios. Con -type d buscará directorios vacios)</p>

<p>$ find / -name &#8220;algo&#8221; -a ! -path &#8220;/media/servidor/*&#8221; -exec echo {} \; 2>/dev/null (Buscar &#8220;algo&#8221; descartando el directorio /media/servidor/)</p>

<p>$ find /home/usuario -iname &#8216;*.odt&#8217; -exec cp &#8216;{}&#8217; /home/usuario/Documentos \; (Copiar todos los odt a la carpeta Documentos)</p></blockquote>
]]></content>
  </entry>
  
</feed>
